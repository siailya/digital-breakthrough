version: "3.9"

services:
  amm-server:
    container_name: amm-server
    image: siailyadev/amm-server:0.0.1
    ports:
      - "8000:8000"

  aam-client:
    container_name: aam-client
    image: siailyadev/amm-client:0.0.1
    ports:
      - "3000:80"